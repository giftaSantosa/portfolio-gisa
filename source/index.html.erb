<%#
  The single page. Every section is a partial in source/partials/ so that each
  one stays small and easy to find. Section order here is the order on screen —
  rearrange these lines to rearrange the page, and update the `nav:` list in
  data/site.yml to match.
-%>
<%= partial "partials/hero" %>
<%= partial "partials/about" %>
<%= partial "partials/projects" %>
<%= partial "partials/skills" %>
<%= partial "partials/contact" %>
